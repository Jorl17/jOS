#ifndef STRING_H
#define STRING_H
#include <stdinc.h>
#include <mem.h> /* memcpy, memset, etc... */

char* strcpy ( char* destination, const char* source );
int strcmp ( const char* str1, const char* str2 );
#endif
